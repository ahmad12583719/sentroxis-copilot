import { useEffect, useMemo, useState } from 'react'
import { ArrowDownUp, CheckCircle2, ChevronRight, Filter, RefreshCw, Search, Shield, SlidersHorizontal } from 'lucide-react'
import { authRequest } from '../auth/AuthProvider'

const displayTime = (timestamp) => timestamp ? new Date(timestamp).toLocaleString([], { hour: '2-digit', minute: '2-digit' }) : '—'

export default function Wazuh({ onSelectAlert }) {
  const [alerts, setAlerts] = useState([])
  const [query, setQuery] = useState('')
  const [severity, setSeverity] = useState('All')
  const [loading, setLoading] = useState(true)
  const [error, setError] = useState('')
  const loadAlerts = () => { setLoading(true); setError(''); authRequest('/api/alerts?source=wazuh&limit=50').then((data) => setAlerts(data.items || [])).catch((requestError) => setError(requestError.message)).finally(() => setLoading(false)) }
  useEffect(() => { loadAlerts() }, [])
  const filtered = useMemo(() => alerts.filter((alert) => { const matchesQuery = `${alert.title} ${alert.agent_name} ${alert.technique}`.toLowerCase().includes(query.toLowerCase()); const matchesSeverity = severity === 'All' || alert.severity === severity.toLowerCase(); return matchesQuery && matchesSeverity }), [alerts, query, severity])
  return <main className="page-shell subpage-shell"><section className="page-heading compact-heading"><div><p className="eyebrow"><Shield size={13} /> Wazuh integration <span className="slash">/</span> authenticated alert stream</p><h1>Wazuh <em>signals.</em></h1><p className="lede">Read-only ingestion surface with server-side authorization and bounded queries.</p></div><div className="heading-actions"><span className={`connection-badge ${alerts.length ? '' : 'pending'}`}><i /> {alerts.length ? 'Telemetry connected' : 'No telemetry'}</span><button className="button secondary" onClick={loadAlerts} disabled={loading}><RefreshCw size={15} /> {loading ? 'Loading…' : 'Refresh'}</button></div></section><section className="panel signal-toolbar"><div className="search-box"><Search size={16} /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search alert, agent, or technique" aria-label="Search signals" /></div><div className="filter-group"><Filter size={15} /><label htmlFor="severity">Severity</label><select id="severity" value={severity} onChange={(event) => setSeverity(event.target.value)}><option>All</option><option>Critical</option><option>High</option><option>Medium</option><option>Low</option></select><button className="icon-button" aria-label="More filters"><SlidersHorizontal size={16} /></button></div></section><section className="panel table-panel"><div className="table-heading"><div><p className="eyebrow">{filtered.length} matching records</p><h2>Alert inventory</h2></div><span className="table-meta"><CheckCircle2 size={14} /> {alerts.length ? 'Server data loaded' : 'Awaiting connection'}</span></div>{error && <p className="empty-state error-state">{error}</p>}{loading && <p className="empty-state">Loading authenticated records…</p>}{!loading && !filtered.length && <p className="empty-state">No Wazuh records match this view. Complete Wazuh setup or connect an authorized alert source.</p>}<div className="table-scroll"><table><thead><tr><th>Signal <ArrowDownUp size={12} /></th><th>Agent</th><th>Severity</th><th>ATT&amp;CK correlation</th><th>Observed</th><th><span className="sr-only">Open</span></th></tr></thead><tbody>{filtered.map((alert) => <tr key={alert.id} onClick={() => onSelectAlert?.(alert)}><td><div className="table-signal"><span className={`severity-pip ${alert.severity}`} /><div><strong>{alert.title}</strong><small>Rule {alert.rule_id}</small></div></div></td><td><strong>{alert.agent_name}</strong><small className="table-sub">{alert.agent_ip || 'IP unavailable'}</small></td><td><span className={`severity-label ${alert.severity}`}>{alert.severity}</span></td><td><span className="technique-chip">{alert.technique}</span><small className="table-sub">{alert.mitre_name}</small></td><td className="muted">{displayTime(alert.timestamp)} UTC</td><td><ChevronRight size={16} className="row-chevron" /></td></tr>)}</tbody></table></div><div className="panel-footer"><span>Showing bounded results · 50 max per request</span><span>Telemetry is untrusted data</span></div></section><section className="callout-grid"><div className="security-callout"><div className="callout-icon"><Shield size={17} /></div><div><strong>Read-only by default</strong><p>Response actions are never executed from telemetry or AI output. Any containment proposal requires a separate approval workflow.</p></div></div><div className="security-callout"><div className="callout-icon violet"><Filter size={17} /></div><div><strong>Normalization boundary</strong><p>Vendor payloads are transformed into stable Alert models before they enter the case or AI layers.</p></div></div></section></main>
}
