import { useEffect, useMemo, useState } from 'react'
import { animate } from 'animejs'
import { ArrowUpRight, Bot, ChevronRight, CircleAlert, Clock3, Crosshair, Database, LockKeyhole, Radar, ShieldAlert, Sparkles, TimerReset, Zap } from 'lucide-react'
import AnimatedCard from '../components/AnimatedCard'
import { authRequest } from '../auth/AuthProvider'

const tactics = ['Reconnaissance', 'Resource Development', 'Initial Access', 'Execution', 'Persistence', 'Privilege Escalation', 'Defense Evasion', 'Credential Access', 'Discovery', 'Lateral Movement', 'Collection', 'Command & Control', 'Exfiltration', 'Impact']
const techniques = ['T1059.001', 'T1053.005', 'T1003', 'T1562.001', 'T1087', 'T1021.001', 'T1105', 'T1041']
const displayTime = (timestamp) => timestamp ? new Date(timestamp).toLocaleString([], { hour: '2-digit', minute: '2-digit' }) : '—'

function MitreMatrix({ alerts, selectedAlert }) {
  const active = useMemo(() => new Set(alerts.map((item) => item.technique)), [alerts])
  useEffect(() => {
    if (window.matchMedia?.('(prefers-reduced-motion: reduce)').matches) return
    const animation = animate('.mitre-cell.is-active', { backgroundColor: ['#192d3a', '#2df7b5', '#143530'], boxShadow: ['0 0 0 rgba(45,247,181,0)', '0 0 18px rgba(45,247,181,.65)', '0 0 8px rgba(45,247,181,.2)'], duration: 850, delay: (_, i) => i * 90, ease: 'inOut(2)' })
    return () => animation?.pause?.()
  }, [selectedAlert, alerts.length])
  return <div className="matrix-wrap"><div className="matrix-header"><span>ATT&amp;CK enterprise coverage</span><span className="matrix-legend"><i className="legend-active" /> correlated <i className="legend-idle" /> observed</span></div><div className="mitre-matrix" role="grid" aria-label="MITRE ATT&CK correlation matrix"><div className="matrix-tactic-labels">{tactics.map((tactic) => <span key={tactic}>{tactic}</span>)}</div><div className="matrix-grid">{tactics.map((tactic) => <div className="matrix-column" key={tactic}>{techniques.map((technique, index) => { const isActive = active.has(technique) && ((index + tactic.length) % 4 === 0 || technique === selectedAlert?.technique); return <span key={`${tactic}-${technique}`} className={`mitre-cell ${isActive ? 'is-active' : ''}`} title={`${tactic} / ${technique}`} role="gridcell">{isActive ? '•' : ''}</span> })}</div>)}</div></div></div>
}

function Metric({ icon: Icon, label, value, delta, tone, detail, index }) {
  return <AnimatedCard index={index} className={`metric-${tone}`}><div className="metric-top"><span className="metric-icon"><Icon size={18} /></span><span className="metric-delta"><ArrowUpRight size={13} /> {delta}</span></div><div className="metric-value">{value}</div><div className="metric-label">{label}</div><div className="metric-detail">{detail}</div></AnimatedCard>
}

export default function Dashboard({ onNavigate, onSelectAlert }) {
  const [alerts, setAlerts] = useState([])
  const [selectedAlert, setSelectedAlert] = useState(null)
  const [loading, setLoading] = useState(true)
  const [error, setError] = useState('')
  useEffect(() => { authRequest('/api/alerts?limit=50').then((data) => { setAlerts(data.items || []); setSelectedAlert(data.items?.[0] || null) }).catch((requestError) => setError(requestError.message)).finally(() => setLoading(false)) }, [])
  const select = (alert) => { setSelectedAlert(alert); onSelectAlert?.(alert) }
  const openCount = alerts.filter((alert) => alert.status !== 'resolved').length
  const criticalCount = alerts.filter((alert) => alert.severity === 'critical').length
  const highCount = alerts.filter((alert) => alert.severity === 'high').length
  const techniquesObserved = new Set(alerts.map((alert) => alert.technique)).size
  return <main className="page-shell"><section className="page-heading"><div><p className="eyebrow"><span className="pulse-dot" /> Live command view <span className="slash">/</span> {new Date().toLocaleDateString()}</p><h1>Good morning, <em>analyst.</em></h1><p className="lede">{loading ? 'Loading authenticated telemetry.' : alerts.length ? <><strong>{alerts.length} signals</strong> are available for analyst review.</> : 'No telemetry is connected yet. Complete server setup to begin.'}</p></div><div className="heading-actions"><button className="button secondary" onClick={() => onNavigate('ai')}><Bot size={16} /> Ask co-pilot</button><button className="button primary" onClick={() => onNavigate('wazuh')}><Radar size={16} /> Review signals <ChevronRight size={15} /></button></div></section><section className="metrics-grid" aria-label="Operational metrics"><Metric icon={ShieldAlert} label="Open incidents" value={openCount} delta="—" detail={`${criticalCount} critical · ${highCount} high`} tone="red" index={0} /><Metric icon={Zap} label="Mean time to detect" value="—" delta="—" detail="Awaiting connected telemetry" tone="teal" index={1} /><Metric icon={TimerReset} label="Mean time to respond" value="—" delta="—" detail="Awaiting case activity" tone="amber" index={2} /><Metric icon={Crosshair} label="ATT&CK coverage" value={techniquesObserved ? `${techniquesObserved}` : '—'} delta="—" detail={techniquesObserved ? 'techniques observed' : 'No correlated techniques'} tone="violet" index={3} /></section><section className="dashboard-grid"><div className="panel alert-panel"><div className="panel-heading"><div><p className="eyebrow">Detection queue</p><h2>Latest signals</h2></div><button className="text-button" onClick={() => onNavigate('wazuh')}>View all <ArrowUpRight size={14} /></button></div>{error && <p className="empty-state error-state">{error}</p>}{loading && <p className="empty-state">Loading signals…</p>}{!loading && !alerts.length && <p className="empty-state">No alert records are available. Connect Wazuh or ingest an authorized signal to populate this queue.</p>}<div className="alert-list">{alerts.slice(0, 6).map((alert) => <button key={alert.id} className={`alert-row ${selectedAlert?.id === alert.id ? 'selected' : ''}`} onClick={() => select(alert)}><span className={`severity-pip ${alert.severity}`} aria-label={`${alert.severity} severity`} /><span className="alert-main"><strong>{alert.title}</strong><small>{alert.agent_name} <span>·</span> {alert.agent_ip || 'IP unavailable'}</small></span><span className="alert-tech"><b>{alert.technique}</b><small>{alert.tactic}</small></span><span className="alert-time">{displayTime(alert.timestamp)}</span><ChevronRight size={15} className="row-chevron" /></button>)}</div><div className="panel-footer"><span><CircleAlert size={14} /> Bounded server-side results</span><span className="freshness"><i /> {alerts.length ? 'Connected' : 'Waiting'}</span></div></div><div className="panel ai-brief-panel"><div className="panel-heading"><div><p className="eyebrow">Evidence-grounded</p><h2>Co-pilot brief</h2></div><span className="ai-orb"><Sparkles size={16} /></span></div>{selectedAlert ? <><div className="ai-brief"><div className="avatar ai-avatar">SX</div><div><p className="ai-label">Sentroxis AI <span>advisory</span></p><p>Signals cluster around <strong>{selectedAlert.technique}</strong> on <strong>{selectedAlert.agent_name}</strong>. The current pattern is mapped to {selectedAlert.tactic.toLowerCase()} activity.</p><p className="ai-note">Next best step: validate the process tree, then collect read-only endpoint context before any containment decision.</p></div></div><div className="confidence"><div><span>Correlation confidence</span><strong>Advisory</strong></div><div className="confidence-bar"><i style={{ width: '82%' }} /></div></div><button className="button full-width" onClick={() => onNavigate('ai')}>Open investigation brief <ArrowUpRight size={14} /></button></> : <p className="empty-state">Select a connected alert to create an evidence-grounded brief.</p>}</div></section><section className="panel matrix-panel"><div className="panel-heading"><div><p className="eyebrow">Threat coverage</p><h2>MITRE ATT&amp;CK matrix</h2></div><div className="matrix-stat"><span className="stat-number">{techniquesObserved || '—'}</span><span>techniques<br />observed</span></div></div><MitreMatrix alerts={alerts} selectedAlert={selectedAlert} /></section><section className="bottom-strip"><div className="mini-status"><Database size={16} /><div><strong>Data plane</strong><span>{alerts.length ? 'Authenticated telemetry connected' : 'Awaiting Wazuh connection'}</span></div><i className={alerts.length ? 'status-ok' : 'status-pending'} /></div><div className="mini-status"><LockKeyhole size={16} /><div><strong>Evidence vault</strong><span>Read-only provenance boundary</span></div><i className="status-pending" /></div><div className="mini-status"><Clock3 size={16} /><div><strong>Last sync</strong><span>{alerts.length ? 'Server data loaded' : 'No sync recorded'}</span></div><i className={alerts.length ? 'status-ok' : 'status-pending'} /></div></section></main>
}
